/* 
** Given a string, return a new string that replaces every occurrence of the 
** word "important" with "urgent":
*/

let advice = "Few things in life are as important as house training your pet dinosaur.";

// To replace "important" with "urgent" we can use the `replace` method
advice.replace('important', 'urgent');


/* To figure out how to replace all occurrences, we can use the `replaceAll` method.
** let test1 = advice.replaceAll("important", "urgent");
*/
